<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.Project" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here -->
        <item name="colorPrimary">@color/teal_700</item>
        <item name="colorPrimaryContainer">@color/teal_200</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Default Button Style Override -->
        <item name="buttonStyle">@style/CustomButtonStyle</item>
    </style>

    <style name="Theme.Project" parent="Base.Theme.Project" />

    <!-- Custom Button Style -->
    <style name="CustomButtonStyle" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@android:color/transparent</item> <!-- Transparent background tint -->
        <item name="android:background">@drawable/button_blue_rounded</item> <!-- Custom drawable -->
        <item name="android:textColor">@android:color/white</item>
    </style>
</resources>
